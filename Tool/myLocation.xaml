<Page
    x:Class="Tool.myLocation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tool"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid Grid.Column="1" Grid.Row="0" Background="Bisque">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" >
                <StackPanel Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    
                    <TextBlock x:Name="tblExactLocation"  Visibility="Collapsed" Text="" FontSize="25" FontFamily="Ash" FontWeight="Bold" Foreground="#525C65" HorizontalAlignment="Center"/>

                    <TextBlock x:Name="tblLocation2" Margin="0,0,0,0" FontFamily="Ash" FontSize="25"/>

                    <Button x:Name="btnLocation"  Content="Show current location" Foreground="Black" Tapped="btnLocation_Tapped"  />

                    <Button x:Name="btnSave" Margin="0,20,0,0" Click="btnSave_Click" Content="Save current location" />

                    
                </StackPanel>
            </ScrollViewer>
        </Grid>

    </Grid>
</Page>
